# Database
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/hype_market?schema=public"

# JWT / Auth
JWT_SECRET="replace-with-strong-secret"

# SMTP (emails) - Configuration requise pour l'envoi d'emails OTP
SMTP_HOST="smtp.yourprovider.com"
SMTP_PORT="587"
SMTP_USER="user@example.com"
SMTP_PASS="your-smtp-password"
FROM_EMAIL="no-reply@hypemarket.ci"

# Exemples de configuration SMTP :
# Hostinger: smtp.hostinger.com:587
# Gmail: smtp.gmail.com:587 (nécessite un mot de passe d'application)
# Brevo: smtp-relay.brevo.com:587

# Live (optional)
LIVEKIT_API_KEY=""
LIVEKIT_API_SECRET=""
LIVEKIT_URL=""
